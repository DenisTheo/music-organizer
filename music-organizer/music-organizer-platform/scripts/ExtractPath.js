
logger.log("extracting file path");

// Fetches the path
var fullPath = document.properties[""] || [];

if ()
{
	// updates the node's path
	;

	// Commit the changes to the node
	try {
		document.save();
		logger.log("Succesfully extracted node path");
	} catch (e) {
		logger.log("Error extracting node path: " + e.message);
	}
}
else
	logger.log("");
